from pathlib import Path

def handle_create(tokens):
    if len(tokens) != 4:
        print("ERROR")
        return

    if tokens[2] != "-n":
        print("ERROR")
        return

    directory = Path(tokens[1])
    filename = tokens[3]
    new_file = directory / f"{filename}.dsu"
    
    if not path.exists() or file_path.exists():
        print("ERROR")
        return
    
    if new_file.exists():
        print("ERROR")
        return

    try:
        new_file.touch()
        print(new_file)
    except Exception:
        print("ERROR")
